clear;
clc;


a=-1.6598;

    


for j=1:6;
    
    theta(j)=(2^(j-1))*a;
    poo(j)=mod((2^(j-1))*a,-2*pi);
    poop(j)=mod(poo(j),pi)/(2*pi) *  90;
    
%HWP(j)=mod((2^(j-1))*a,-2*pi)/(-2*pi)   *90;

end
theta
poo
poopa
